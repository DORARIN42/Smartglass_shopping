# 메타 AR 쇼핑 도우미 개발 계획서

- **버전:** v0.1
- **팀 구성:** 디자인·기획 1명, 개발 3명
- **개발 방향:** Android 스마트폰으로 선개발한 뒤 Meta 글라스에 최적화
- **예상 기간:** 6주 MVP

## 1. 프로젝트 목표

사용자가 바라보는 상품을 촬영하면 상품을 인식하고, 상품 정보·가격·판매처·유사 상품·구매 링크를 제공하는 쇼핑 도우미를 개발한다.

현재 글라스가 없으므로 Android 스마트폰 후면 카메라로 기능을 먼저 구현한다. 이후에는 핵심 기능을 다시 만들지 않고 **카메라·입력·출력 어댑터만 글라스용으로 교체**한다.

## 2. MVP 범위

### 포함

- Android 카메라 프리뷰 및 사진 촬영
- 이미지 업로드와 분석 상태 표시
- 바코드·상품 라벨 텍스트 추출
- 브랜드, 카테고리, 색상, 검색 키워드 생성
- 상품 후보 최대 3개 제공
- 가격·판매처·상품 링크 표시
- 불확실 결과, 재촬영, 네트워크 오류 처리
- 스마트폰 결과·상세·간단한 히스토리 화면
- 글라스용 간소화 UI 시뮬레이션

### 후속 개발

- 실시간 연속 영상 분석
- 모든 상품의 정확한 SKU 자동 확정
- 리뷰 크롤링 및 자동 요약
- 결제·주문 기능
- 개인화 추천
- 실제 글라스의 배터리·발열·가독성 최적화

## 3. 권장 구조

```text
현재
Android 카메라
  → PhoneCameraCaptureSource
  → 상품 분석
  → 상품 검색
  → 스마트폰 UI

글라스 확보 후
Meta 글라스 카메라
  → MetaGlassesCaptureSource
  → 기존 상품 분석·검색 재사용
  → 글라스 요약 UI + 스마트폰 상세 UI
```

교체 가능한 영역은 다음 세 가지로 제한한다.

```kotlin
interface CaptureSource
interface UserActionSource
interface ResultPresenter
```

현재는 스마트폰 구현체를 사용하고, 글라스 확보 후 Meta 전용 구현체를 추가한다.

## 4. 업무 배분

| 담당 | 주요 업무 | 소유 영역 |
|---|---|---|
| A. 디자인·기획 | 유저 플로우, 화면, 문구, 상태표, 테스트 시나리오, 디자인 QA | `/design`, `/docs/ux` |
| B. Android·통합 | 앱 구조, CameraX, 상태 관리, 네트워크, Navigation, 빌드, 추후 Meta SDK | `/android/app`, `/android/core`, `/feature/capture` |
| C. AI·인식 | 이미지 분석 API, OCR·바코드 처리, AI 속성 추출, 신뢰도·재촬영 판단 | `/server/recognition`, `/server/ai` |
| D. 상품 검색·결과 | 쇼핑 API, 후보 랭킹, 가격 비교, 상품 링크, 결과·상세·히스토리 UI | `/server/commerce`, `/feature/result`, `/feature/history` |

### 충돌 방지 원칙

- `MainActivity`, Navigation, Gradle, Manifest, 공통 모델은 B가 관리한다.
- C와 D는 Android 공통 파일을 직접 수정하지 않는다.
- 각 담당자는 자신의 모듈 안에서만 개발한다.
- 앱과 서버는 고정된 OpenAPI·JSON 계약으로 연결한다.
- 결과 화면은 D가 개발하고, 앱 연결은 B가 담당한다.

## 5. 개발 순서

### 1주 차 — 구조·계약 확정

- 상태 목록과 화면 전환 정의
- OpenAPI와 오류 코드 확정
- Android 프로젝트와 서버 기본 구조 생성
- 가짜 데이터로 `촬영 → 분석 → 상품 결과` 전체 흐름 연결

**산출물:** 앱 골격, API 명세, Mock 시연

### 2주 차 — 촬영·업로드

- CameraX 프리뷰와 촬영
- 권한, 이미지 압축, 저장 및 업로드
- 로딩·재시도·오류 화면
- 결과 화면 Mock UI

**산출물:** 스마트폰 촬영 및 업로드 기능

### 3주 차 — 인식·상품 검색

- 바코드 및 OCR 처리
- 비전 AI 구조화 출력
- 쇼핑 API 연동
- 후보 정규화·중복 제거·랭킹

**산출물:** 실제 분석 결과와 상품 후보 응답

### 4주 차 — 실제 API 통합

- Mock을 실제 API로 교체
- 결과·상세·히스토리 화면 연결
- 불확실 결과와 재촬영 흐름 구현

**산출물:** End-to-End 베타

### 5주 차 — POV 테스트

- 눈높이 후면 카메라 테스트
- 거리, 조도, 흔들림, 반사 소재, 다중 상품 테스트
- 인식 정확도와 검색 품질 개선
- 네트워크 및 서버 오류 처리 점검

**산출물:** 테스트 리포트와 수정 버전

### 6주 차 — MVP 마무리

- 회귀 테스트와 배포
- API·환경 변수·운영 문서 정리
- Meta 글라스 어댑터 명세 작성
- 실기기 확보 후 검증 체크리스트 작성

**산출물:** MVP APK, 서버, 이관 문서

## 6. 권장 기술 및 API

| 영역 | 권장 기술 |
|---|---|
| Android | Kotlin, Jetpack Compose, CameraX, Coroutines/Flow, Hilt, Retrofit/OkHttp, Room |
| 기기 내 분석 | Google ML Kit Barcode Scanning, Text Recognition |
| 비전 AI | 이미지 입력과 구조화 JSON 출력을 지원하는 Gemini 계열 모델 |
| 상품 검색 | NAVER 쇼핑 검색 API 또는 NAVER API HUB |
| 백엔드 | Python FastAPI, Pydantic, PostgreSQL, Object Storage |
| 배포 | Cloud Run 또는 유사한 컨테이너 환경 |
| 글라스 연동 | Meta Wearables Device Access Toolkit for Android |

상품 분석 순서는 아래와 같이 구성한다.

```text
바코드 탐지
  → OCR 텍스트 추출
  → 비전 AI 상품 속성 분석
  → 쇼핑 API 검색
  → 후보 랭킹
  → 사용자에게 최대 3개 제시
```

AI는 상품명이나 가격을 생성하지 않고 **검색에 필요한 속성과 키워드만 생성**한다. 실제 상품명·가격·링크는 쇼핑 API 결과를 사용한다.

## 7. 주요 상태

| 상태 | 처리 |
|---|---|
| READY | 촬영 준비 및 가이드 표시 |
| ANALYZING | 분석 진행 상태 표시 |
| MATCHED | 대표 상품과 후보 표시 |
| UNCERTAIN | 후보 2~3개 중 사용자 선택 |
| RETRY_REQUIRED | 어두움, 흔들림, 검색 단서 부족 안내 |
| ERROR | 네트워크·서버·권한 오류 및 재시도 |

## 8. 협업 방식

- 기능 브랜치는 담당 영역 기준으로 짧게 운영한다.
- PR은 가능하면 1~2일 단위로 작게 병합한다.
- 공통 파일 변경은 B의 리뷰를 필수로 한다.
- API 필드 변경 시 OpenAPI, Mock JSON, 서버 테스트를 함께 수정한다.
- 매주 실제 Android 기기에서 전체 흐름을 시연한다.

## 9. 글라스 확보 후 추가 작업

- `PhoneCameraCaptureSource`를 `MetaGlassesCaptureSource`로 교체
- 터치 입력을 버튼·음성·제스처 입력으로 연결
- 글라스에는 상품명, 최저가, 스마트폰 확인 안내만 표시
- 실제 시야각, 해상도, 지연, 연결 끊김, 배터리, 발열, 가독성 재검증

## 10. 완료 기준

- Android 기기에서 촬영부터 상품 링크 확인까지 완료된다.
- AI나 쇼핑 API가 실패해도 앱이 종료되지 않는다.
- 불확실한 상품을 하나로 단정하지 않고 후보를 제공한다.
- 담당자별 코드 소유 영역이 명확하게 유지된다.
- OpenAPI, 서버 응답, Android 모델이 일치한다.
- 글라스 연동 시 추가할 파일과 테스트 항목이 문서화되어 있다.

> 외부 SDK, AI 모델, 쇼핑 API 정책은 변경될 수 있으므로 실제 개발 착수 전에 공식 문서와 이용 조건을 다시 확인한다.
